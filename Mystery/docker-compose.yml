services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: mystery_sql
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "MysterySql2025!"
      MSSQL_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - .:/mystery
    restart: unless-stopped
